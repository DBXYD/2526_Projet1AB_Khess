Brouillon pour Estelle : 

Le bloc noir derrière l'écran est une carte de controle LCD. 
Elle sert d'intermédiaire entre la rasberry pi et l'écran.
Deux sortie : 
- un port HDMI relié a la rasberry pi (port HDMI sur la rasberry)
- un port micro-USB pour l'alimetation (c'est la rasberry pi qui alimente l'écran, on doit aussi la reliée a la rasberry pi, port USB A sur la rasberry)
Le choix recommandé : Raspberry Pi 4 Model B

Le cable de la caisse : il y a 4 pin.
pin vert pour ouvrir la caisse 
pin noir relié a la masse

ce que le prof a dit : 
nos donnée sous forme de liste, il faut mieux les organiser, on peut utiliser dbdiagramme en ligne. 
Lorsque qqn essaye de ce connecté, le serveur cherche si il est dans user_custom, si il n'y est pas il y a error, il faut que si il n'y ai pas qu'il le crée. 

Django : 
python3 -m venv venv
source venv/bin/activate
pip install django
pip install django-cas-ng
cd DatabaseCashRegister
python3 manage.py runserver

Pour voir la base de donée sur Vs code : installerr SQlite et SQlite viewver.
Pour se crée un compte admin : 
python3 manage.py shell
from django.contrib.auth.models import User
u = User.objects.get(username='ton_nom_d'utilisateur_ensea')
u.is_staff = True
u.is_superuser = True
u.save()
exit()

lien pour nous aider, projet des bachelors : 
https://github.com/DBXYD/ENSEA_B2_Web
